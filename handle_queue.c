#include "monty.h"
/**
 * _queue - mode add from bottom
 * @stack: stack head
 * @line_number: line_number
 * Return: no return
*/
void _queue(stack_t **stack, unsigned int line_number)
{
	NOTUSED(*stack);
	NOTUSED(line_number);

	glob_buffer.mode = 1;
}
