#include "monty.h"
/**
 * _stack - mode add from top
 * @stack: stack head
 * @line_number: line_number
 * Return: no return
*/
void _stack(stack_t **stack, unsigned int line_number)
{
	NOTUSED(*stack);
	NOTUSED(line_number);

	glob_buffer.mode = 0;
}