#include "monty.h"
#define UNSUED(x) (void)(x)
/**
*_nop - do nothing
*@stack: pointer to pointer to head of stack
*@line_number: line at which opcod is found
*/
void _nop(stack_t **stack, unsigned int line_number)
{
	NOTUSED (stack);
	NOTUSED (line_number);
}
